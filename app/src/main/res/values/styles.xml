<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="LightEditText" parent="Widget.AppCompat.EditText">
        <item name="android:textColorHint">#aafafafa</item>
        <item name="android:textColor">@color/dark_bg_light_primary_text</item>
        <item name="android:backgroundTint">#aafafafa</item>
    </style>

    <style name="LightTextInput" parent="Widget.Design.TextInputLayout">
        <item name="android:textColorHint">#aafafafa</item>
        <item name="android:textColor">@color/dark_bg_light_primary_text</item>
        <item name="android:color">@color/dark_bg_light_primary_text</item>
        <item name="android:textSize">20sp</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="colorControlNormal">#aafafafa</item>
        <item name="colorControlActivated">#aafafafa</item>
    </style>

    <style name="AppTheme.SplashTheme" parent="AppTheme.NoActionBar">
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowBackground">@drawable/gradient_splashscreen</item>
    </style>

    <style name="AppTheme.SignInTheme" parent="AppTheme.NoActionBar">
        <item name="android:textColor">@color/dark_bg_light_primary_text</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowBackground">@color/colorPrimary</item>
    </style>

</resources>
