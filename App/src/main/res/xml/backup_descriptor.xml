<?xml version="1.0" encoding="utf-8"?>
<full-backup-content>
    <!-- TODO Remove the following "exclude" elements to make them a part of the auto backup -->
    <!-- Exclude the shared preferences file that contains the GCM registrationId -->
    <exclude
        domain="sharedpref"
        path="it.home2work.app.session.xml" />
    <exclude
        domain="sharedpref"
        path="it.fleetup.app.preferences.xml" />
    <exclude
        domain="sharedpref"
        path="it.home2work.app.signin.xml" />
</full-backup-content>
